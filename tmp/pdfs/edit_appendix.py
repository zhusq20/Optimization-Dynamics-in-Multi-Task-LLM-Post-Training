from pathlib import Path

def revise(path, pairs):
 p=Path(path);s=p.read_text()
 for a,b in pairs:
  if a not in s:print('MISSING',path,a[:80])
  else:s=s.replace(a,b)
 p.write_text(s)

revise('sections/005_experiment.tex',[
('The experiments compare averaging rules, teacher-conditioned updates, and vocabulary supervision using a shared student initialization and four domain teachers.','All experiments start from the same student initialization and use four domain-specific teachers. We compare loss averaging rules, optimizer updates from different teachers, and the number of vocabulary tokens included in the distillation loss.'),
('the original PG-DR and I64-DR/DT/GT branches','PG-DR and I64-DR/DT/GT'),
('The frozen protocol records model revisions and weight identities.','The experiment records identify the model revisions and weight files.'),
('aggregates 64 responses across all domain blocks','aggregates 64 responses from the four domains'),
('single-teacher comparison','mathematics-only comparison'),
('plus the shared initial evaluation. Appendix~\\ref{app:independent_domain_test} describes a separate additional-domain test.','plus the initial student evaluation. Appendix~\\ref{app:independent_domain_test} describes additional test sets for the same four domains.'),
('\\caption{Supplementary seed-42 capability scores (\\%). S denotes mathematics-only training and M denotes joint training; both use DR. Single-teacher trajectories and intermediate joint checkpoints complement the initial and joint endpoint scores in Table~\\ref{tab:completed_endpoints}.}', '\\caption{\\textbf{PG/I64 performance depends on the task and checkpoint.} Seed-42 scores (\\%): S is mathematics-only training and M is joint training; both use DR. Joint endpoints appear in Table~\\ref{tab:completed_endpoints}.}'),
('These scores describe the available supervision on each target domain.','These scores quantify each teacher\'s performance on its target domain.'),
('\\caption{Domain teacher reference scores (\\%). Science uses GPQA average@4.}','\\caption{\\textbf{Reference scores for the four domain-specific teachers} (\\%). Science uses GPQA average@4.}'),
('\\subsection{Paired comparisons and evidence provenance}','\\subsection{Paired comparisons and experiment records}'),
('Each GPQA question retains','Each GPQA question retains'),
('conditional on the trained checkpoints.','for the evaluated checkpoints.'),
('The same procedure supplies the initial-to-checkpoint comparisons in the evidence bundle.','We use the same procedure for comparisons with the initial student.'),
('\\caption{Update-500 I64 capability contrasts against DR, with nominal 95\\% paired question-bootstrap intervals.}','\\caption{\\textbf{I64 averaging rules produce task-dependent score differences.} Update-500 differences from DR with nominal 95\\% paired question-bootstrap intervals.}'),
('\\caption{Update-500 I64-minus-PG capability differences and nominal 95\\% paired question-bootstrap intervals (percentage points). All eight intervals include zero.}','\\caption{\\textbf{All eight I64-minus-PG intervals include zero.} Update-500 score differences and nominal 95\\% paired question-bootstrap intervals (percentage points).}'),
('The evidence bundles retain frozen scores, question-level records or verified completion summaries, model and data identities, and the analysis scripts. The original benchmarks and expanded matrix share checkpoints and are not independent inventories. Training-seed variation, question-bootstrap uncertainty, and repeated local response banks are reported separately throughout.','The experiment archive retains scores, question-level results or verified run summaries, model and data identifiers, and analysis scripts. The initial and expanded evaluations reuse some checkpoints and should not be counted as independent runs. We distinguish uncertainty from training seeds, evaluation questions, and repeated sampling of cached response batches (called \\emph{banks} in the figures).'),
])

revise('sections/009_appendix.tex',[
('Any PG coefficients are detached.','The PG log-ratio coefficients are treated as constants during backpropagation.'),
('Dividing by $\\bar T_d$ yields','Dividing by $\\bar T_d$ gives'),
('domain weights $\\lambda_d$','domain loss weights $\\lambda_d$'),
('three banks (seeds 1042, 1043, and 1044) each contain eight responses per domain','three cached response batches (seeds 1042, 1043, and 1044) each contain eight responses per domain'),
('For each bank,','For each batch,'),
('All three reductions','All three averaging rules'),
('Student and optimizer snapshots match within each checkpoint.','Each comparison starts from the same student parameters and Adam first and second moments.'),
('over all 24 bank-domain groups','for all 24 batch-domain groups'),
('The observed gradient directions establish that the covariance contribution is nonzero on these batches.','The measured cosine differences show that response length and gradient direction are correlated in these batches.'),
('reuse these six banks','reuse these six batches'),
('for that bank','for that batch'),
('optimizer clock','optimizer step counter'),
('retaining the clock','retaining the step counter'),
("that reduction's saved-state Adam FP32 step norm","the FP32 Adam step norm for that averaging rule"),
('Every proposed FP32 step is cast to BF16 before evaluating one held-out response per domain.','After each FP32 update, the model parameters are cast to BF16 and evaluated on one held-out response per domain.'),
('We report full-vocabulary reverse KL for evaluation; the training-consistent I64 surrogate is retained in the source bundle. These are immediate local interventions, not additional online training runs.','Evaluation uses full-vocabulary reverse KL; the experiment archive also retains the I64 surrogate used for training. Each comparison applies a single optimizer step to a checkpoint copy.'),
('\\caption{Fixed-batch averaging comparison. Entries are raw-gradient cosines; each row uses one shared student and response bank. DR and DT share domain weights and differ only in within-domain response weighting.}','\\caption{\\textbf{Equal domain weights do not imply equal gradient directions.} Raw-gradient cosines on identical cached responses. DR and DT use equal domain loss weights; only DT weights responses by length.}'),
('The online record distinguishes raw aggregate gradients, clipped gradients, actual FP32 optimizer steps, cumulative FP32 master changes, and cumulative BF16 model changes. Cumulative changes use the same initialization reference.','Training measurements separately record gradients before and after clipping, FP32 optimizer steps, and cumulative parameter changes in the FP32 master weights and BF16 model weights. Cumulative changes are measured from the same initialization.'),
('excluding vocabulary padding and shared duplicates','excluding vocabulary padding and duplicate entries for tied weights'),
('are retained in the frozen measurements','are retained in the experiment archive'),
('The sparsity value is the fraction at or below a threshold; figures report its complement.','For an absolute threshold $\\tau$, sparsity is the fraction of coordinates with magnitude at most $\\tau$; the figures show the fraction exceeding $\\tau$.'),
('For local calculations,','For checkpoint-based measurements,'),
('step clocks','step counters'),
('Local forward/backward computations','Forward and backward passes'),
('We evaluate three local states.','We compare three Adam states at the same checkpoint.'),
('step clock','step counter'),
('the clock','the step counter'),
('two response banks','two cached response batches'),
('A no-step master-to-BF16 cast gives an exact match to the saved model. Figure~\\ref{fig:thresholds} shows how absolute thresholds change the measured local activity.','Casting the saved FP32 master weights to BF16 without an optimizer step exactly reproduces the saved model. Figure~\\ref{fig:thresholds} shows how the threshold changes the measured fraction of updated coordinates.'),
('The zero-current-gradient branch changes','Setting the current gradient to zero while retaining the trained Adam moments changes'),
('These controls explain why a sparse gradient selection cannot by itself identify the parameters changed by an optimizer step.','The parameters changed by Adam therefore depend on its accumulated moments and BF16 rounding, not just on which entries of the current gradient are nonzero.'),
('\\caption{Threshold sensitivity of FP32 steps and BF16 changes. Lines show bank means; shading spans their range.}','\\caption{\\textbf{Update sparsity depends on the numerical threshold.} FP32 steps and BF16 parameter changes; lines show batch means and shading their range.}'),
('\\subsection{Response banks, overlaps, and gradient magnitudes}','\\subsection{Cached responses, gradient overlap, and gradient norms}'),
('Banks use seeds 42 and 43, one response per domain per bank,','Batches use seeds 42 and 43, one response per domain per batch,'),
("Each row's four prefixes receive equal weight. For routed gradients, each teacher scores its domain row; for common-bank gradients, it scores all four rows with equal domain weights. Crossed comparisons score every domain row with every teacher, keeping the number of prefixes per gradient at four. Crossed and routed/common comparisons share underlying gradients and form paired conditions within banks.","Each response's four prefixes receive equal weight. For domain-specific inputs, each teacher scores the response from its own domain; for shared inputs, each teacher scores all four responses with equal domain weights. A third control evaluates every teacher on every input domain separately, keeping four prefixes per gradient. These conditions reuse the same responses, allowing paired comparisons within each batch."),
("the first bank's instruction-following gradient", "the first batch's instruction-following gradient"),
('supplies the routed PG norms','reports PG gradient norms for domain-specific inputs'),
('Cosine normalizes magnitude, so opposite directions for such a row carry very little absolute gradient mass.','Cosine similarity divides out the gradient norms, so a negative cosine can correspond to gradients that are too small to substantially affect the aggregate update.'),
('gives mean routed Jaccard overlaps','gives mean Jaccard overlaps for domain-specific inputs'),
('across the two banks and six teacher pairs','for the two batches and six teacher pairs'),
('the crossed input control below fixes the count','the teacher-by-input-domain comparison below fixes the prefix count'),
('Holding the teacher fixed while changing the input domain yields','Holding the teacher fixed while changing the input domain gives'),
('For routed gradients','For domain-specific inputs'),
('supplies this input and direction context for the teacher-overlap results','shows how input choice affects the teacher-overlap measurements'),
('\\caption{Input dependence of teacher overlap. Left/center: crossed teacher--input comparisons. Right: top-1\\% gradient Jaccard versus signed cosine for routed and common inputs.}','\\caption{\\textbf{Gradient overlap depends strongly on the input domain.} Left/center: teacher-by-input-domain controls. Right: top-1\\% gradient Jaccard versus cosine on domain-specific or shared inputs.}'),
('\\paragraph{Four-bank local experiment.}','\\paragraph{Single-step comparison using four response batches.}'),
('uses four response banks','uses four cached response batches'),
('Each bank contains','Each batch contains'),
('PG actions are freshly sampled from the student at the cached prefixes. Every loss uses the same prefixes and domain-response weights.','PG actions are independently sampled from the student at these fixed prefixes. All losses use the same prefixes, equal domain weights, and equal response weights within each domain.'),
('\\caption{Local concentration at M-PG/100, averaged over four matched banks. NZ is the percentage of nonzero coordinates; top-1\\% is the percentage of squared step norm carried by the largest 1\\% of coordinates. These are local bank means, not training-seed estimates.}','\\caption{\\textbf{Vocabulary supervision produces similar single-step update concentration.} M-PG/100 means over four cached batches. NZ: nonzero coordinates (\\%); top-1\\%: squared step norm in the largest 1\\% of coordinates (\\%).}'),
('The local loss and Adam implementations are the same as in the two-bank controls','The loss and Adam implementations match the two-batch controls'),
('across all four student states','at all four student checkpoints'),
('\\paragraph{Teacher-top-64 local reference.}','\\paragraph{Teacher-top-64 reference objective.}'),
('T64 and full vocabulary are local objective comparisons; online capability curves compare PG, I16, and I64. I16 has no corresponding local geometry measurement.','T64 and full-vocabulary KL are used only for single-step comparisons. End-to-end training compares PG, I16, and I64; the I16 runs have no corresponding single-step gradient or update measurements.'),
('The identity conditions on prefixes and fixed weights. Original rollout losses additionally use the realized response and its eventual length-dependent weight. Table~\\ref{tab:coverage_mass} reports the corresponding coverage for the two-bank controls used to interpret gradient overlap and optimizer history.','This identity holds for fixed prefixes and weights. Training losses also depend on the sampled response and, under DT or GT, its length. Table~\\ref{tab:coverage_mass} reports candidate-set coverage for the two-batch gradient-overlap and Adam-state controls.'),
('\\caption{Teacher-top-64 coverage and routed gradient scale in the two-bank controls. Probability mass and student entropy (nats) are prefix averages; displayed 100\\% values may reflect rounding.}','\\caption{\\textbf{The teacher top-64 set captures nearly all student probability mass in these controls.} Means over sampled prefixes; gradient norms use domain-specific inputs. Entropy is in nats, and 100\\% may reflect rounding.}'),
('Teacher distance is computed on the full next-token vocabulary at matched prefixes:','We measure teacher distribution differences using Jensen--Shannon divergence on the full next-token vocabulary at identical prefixes:'),
('common-input optimizer-step selections','the largest-magnitude coordinates of optimizer steps on shared inputs'),
('twelve bank-pair JS measurements','twelve batch--teacher-pair JS measurements'),
('its pair-level records remain in the evidence bundle','the per-pair measurements are retained in the experiment archive'),
('within each bank','within each batch'),
])
