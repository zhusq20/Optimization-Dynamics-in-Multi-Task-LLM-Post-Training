\section{Supporting Views for the Main Figures}
\label{app:nine_panel_support}

\paragraph{Capability provenance.}
Figures~\ref{fig:normalization_capability}(c--f) and~\ref{fig:normalization_aggregate_support} summarize the experimental score series for seeds 42, 43, and 44 supplied and confirmed by the authors. Their original means, sample standard deviations, and error bars are retained. The expanded endpoint matrix and Figure~\ref{fig:supervision_overview}(c) use seed-42 scores; these additional contrasts are point estimates rather than tests of training-seed significance.

\paragraph{Teacher-update controls.}
Figure~\ref{fig:subnetwork_overview}(a) compares DR cumulative changes at update 500. Panels (b,c) use the common M-PG/500 student and optimizer state, separating routed and common inputs. Each bank contains six teacher pairs; the displayed bank points and means preserve this grouping. The history-subtracted control uses Equation~\ref{eq:history_subtracted} and reports FP32 update cosines. SGD and reset-$m$ steps are individually norm-matched to saved-state Adam. Teacher-pair observations share teachers and banks and are dependent. Steps use non-fused PyTorch updates on saved-state copies, rather than bitwise replay of the online optimizer. Single-teacher and joint runs differ in per-domain response exposure as specified in Appendix~\ref{app:setup}.

\paragraph{Local supervision controls.}
Figure~\ref{fig:supervision_overview}(a,b) uses four banks per joint-student state. Losses share prefixes and saved optimizer state within each row, while inputs may differ across rows. Direct T64 step-direction pairs were not recorded. Panel (c) matches task, setting, and update for S-I64, M-I64, and the added M-I16 branch, each relative to PG in its own teacher setting. Table~\ref{tab:local_concentration} supplies the FP32 and energy-concentration measurements for the M-PG/100 comparison described in Appendix~\ref{app:losses}.

\subsection{Additional control figures}
\label{app:added_controls}

Figure~\ref{fig:normalization_interventions} reuses the six fixed banks and online clipping records described in Appendix~\ref{app:fixed_batch_normalization}. Reset-$m$ and reset-$(m,v)$ retain the optimizer clock. Panel (c) averages four domain probe losses within each bank; its error bars are sample standard deviations across the three banks at each checkpoint, not training-seed uncertainty.

Figure~\ref{fig:sampling_history_controls}(a) uses three M-PG/100 rollout banks with a 4,096-token response cap, containing 48 responses and 288 prefixes. Twelve fixed-prefix subbanks each receive two independent PG action draws at counts 1, 16, and 64; the I64 reference is evaluated once per subbank. Mean PG gradient cosines to full vocabulary are 0.5374, 0.8652, and 0.9667; I64 is 0.999988. These are local diagnostic repeats. Panels (b,c) instead use the two 256-token-cap banks from Appendix~\ref{app:records}; the zero-gradient branch is measured once. Reset-$m$ retains the second moment and clock, whereas fresh Adam resets both moments and the clock. These separate experiments do not form one paired intervention chain.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/nine_panel_20260914/section3_capability_support.pdf}
\caption{Aggregate GT/DT/DR capability: (a) four-domain mean with student/teacher references; (b) worst-domain change from initialization. Zero denotes no regression. Error bars show mean $\pm$ sample SD across the three reported experimental seeds.}
\label{fig:normalization_aggregate_support}
\end{figure}

\paragraph{Cumulative trajectories.}
Figure~\ref{fig:geometry} extends the update-500 summary in Figure~\ref{fig:subnetwork_overview}(a) to the recorded trajectories. Single-teacher geometry uses the previously verified shared measurements and provenance snapshot; those remote model tensors were not remeasured for this appendix. The single-teacher runs receive four times as many mathematics responses per update as the joint runs.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/appendix_mvp_20260914/cumulative_geometry.pdf}
\caption{Cumulative PG/I64 changes through update 500 in single-teacher (S) and joint (M) training. (a,b) Nonzero fractions in FP32 and BF16. (c,d) Coordinate fractions carrying 90\% of squared change in each precision. Lines connect measured updates 1, 50, 100, 250, and 500; joint I64 uses DR.}
\label{fig:geometry}
\end{figure}

\section{Recipe Details and Validation Scope}
\label{app:recipe_details}

With the retention goal fixed, the averaging rule should express the intended importance of each training response. For an objective that weights responses equally within each domain, we use DR with explicit domain weights $\lambda_d$. In our four-domain setting, equal weights and 16 prompts per domain provide the starting allocation. If longer responses are deliberately assigned more weight within a domain, DT expresses that alternative. Under either reduction, the prompt quota and the domain loss weight remain separate choices: with every domain represented and its loss averaged separately, increasing a quota at fixed $\lambda_d$ changes exposure and estimation variance without directly increasing that domain's expected gradient weight. Generated lengths and truncation rates should therefore be recorded alongside both quantities when diagnosing an imbalance.

Once these weights are explicit, vocabulary feedback can be chosen using its capability profile and computational cost. The joint comparison in Section~\ref{sec:density} motivates I64 as a candidate alongside DR, with PG retained as a reference. Its feedback scale also deserves attention. For the student candidate set $S$ and teacher intersection $I$ in Equation~\ref{eq:topk}, the retained student weights sum to $r(h)=p_\theta(I\mid h)/p_\theta(S\mid h)$. At a fixed prefix with a nonempty intersection and detached coefficients, the I64 gradient is $r(h)$ times the gradient obtained by renormalizing the same weights over $I$. Thus removing length weighting does not remove variation in feedback scale across prefixes. Monitoring this retained mass and empty intersections by domain helps assess whether a fixed candidate size supplies comparable coverage; increasing or renormalizing a small intersection requires validation because it can also amplify unreliable feedback.

If a domain repeatedly falls below its retention tolerance, the response should depend on the diagnosed cause. Exposure, length, and coverage checks can reveal inadequate sampling or unintended weighting. When these do not explain the decline, teacher interactions should be examined at a common student and optimizer state. The overlap diagnostic defined in Section~\ref{sec:subnetworks} describes shared update locations; a further functional check is to apply each teacher's proposed step and measure the change in fixed probe losses from all domains. A zero-current-gradient step provides a reference for the contribution of optimizer history. This comparison can identify local cross-domain harm that coordinate overlap alone does not establish, while subsequent capability evaluations determine whether it persists along training. Gradient modification or task-specific parameters become interventions to test when such harm remains reproducible after the allocation and coverage checks.

\paragraph{Completed evidence and remaining validation.}
Appendix~\ref{app:completed_results} reports the completed seed-42 GT/DT/DR $\times$ PG/I64 endpoints, the I16 and SGD extensions, independent testing, checkpoint selection, and the actual I64 intersection-mass audit. The retention constraint does not change the maximum-utility selection in these trajectories. The expanded matrix does not provide matched repeated-seed or matched-compute evidence, and teacher-conditioned steps have not yet been evaluated on a full cross-domain functional probe matrix. These limits are distinct from the completed capability and geometric measurements.
