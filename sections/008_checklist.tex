\section*{Reproducibility Statement}

The accompanying evidence bundle contains figure and table values, configuration records, source hashes, scoring audits, and CPU plotting scripts. Appendix~\ref{app:setup} gives the training data sources, evaluation protocol, evaluated checkpoints, and uncertainty estimates. Appendix~\ref{app:records} specifies parameter measurements and single-step optimizer controls; Appendix~\ref{app:completed_results} describes the additional test set, checkpoint-selection rules, and sensitivity to known training--evaluation overlap.

\section*{Ethics Statement}

The experiments use public model families and benchmark datasets to study optimization and capability transfer. We report standard benchmark scores with the stated evaluation denominators. Generative AI assisted manuscript editing and analysis code; the authors are responsible for the reported measurements and their interpretation.
