

\subsection*{AI use statement}

We used generative AI tools for language editing and manuscript organization.
The authors reviewed every AI-assisted revision and take responsibility for the
final text, claims, proofs, code, and experimental results.

\subsection*{Ethics statement}

The study evaluates optimization methods for existing language models with
established reasoning and instruction-following datasets and automated
verifiers. Model and dataset biases can affect the reported capability scores.
We record data provenance, invalid outputs, and verifier failures to support
careful interpretation of the results.

\subsection*{Reproducibility statement}

Section~\ref{sec:experiments} specifies the model family, task setup, update
budget, evaluation points, and evaluation protocol. The appendix gives the
sampling derivations and implementation details. The repository includes the
controlled experiment code and generated figure. The large-model release will
include data manifests, model and tokenizer revisions, optimizer settings,
task-sampling logs, saved model states, and per-prompt evaluation outputs for
all three training seeds.
