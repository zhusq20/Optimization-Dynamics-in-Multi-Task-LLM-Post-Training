\subsection*{AI use statement}

Generative AI tools assisted with language editing and manuscript organization.
The authors remain responsible for reviewing the text, claims, proofs, code,
and experimental results before submission.

\subsection*{Ethics statement}

The study evaluates optimization methods for existing language models with
reasoning and instruction-following datasets and automated verifiers. Model and
dataset biases can affect capability scores. The final artifact will record data
provenance, invalid outputs, and verifier failures to support careful
interpretation.

\subsection*{Reproducibility statement}

Section~\ref{sec:experiments} specifies the micro-batch composition, fixed
objective weights, integer allocation, resource accounting, baselines,
held-out evaluation, and matched prompt streams. The current artifact includes
the controlled-check code, seeds, and source data for the controlled figure.
\missing{Before submission, add the frozen run configuration, model and
tokenizer revisions, initial-loss weights, prompt streams, optimizer and
allocation states, micro-batch counts, noise and timing logs, per-prompt
evaluation outputs, and source data for every empirical figure and table.}
