# Introduction 重写建议与英文草稿

2026-09-12。依据当前主论文、完整实验比较及相关论文原文引言整理。本文件供讨论和改稿使用，未替换主论文。

## 1. 推荐的叙事顺序

建议采用：**MOPD 在工业界能力合板中的应用 → MOPD 的工作方式 → 最直接的已有进展 → 明确的未解问题 → 本文的研究方式与主要发现 → 三条贡献及实践启示。**

根据后续讨论，第一句直接说明工业应用，随后点名 Xiaomi MiMo-V2-Flash、NVIDIA Nemotron 3 Ultra、Kimi K3 和 DeepSeek-V4。这样一开始便说明研究对象的现实重要性；原来泛泛介绍“单个模型需要多种能力”的内容可压缩进 MOPD 的用途说明。四份报告均有明确的多教师 on-policy 蒸馏能力整合描述，原文位置见第 8 节。

“先介绍 MOPD，再 review 相关工作，最后讲贡献”这个方向合适；其中最需要补足的是 related work 与 contribution 之间的研究缺口。引言里的 review 应当为这个缺口服务，完整分类和技术比较继续放在 Related Work。

适合本稿的统一问题是：

> 在共享学生中，教师监督如何被加权并转化为参数变化，这些训练选择又如何影响最终的跨领域能力？

对应的三个层次是监督权重、参数变化和能力结果。它们构成同一研究视角；目前的实验尚未建立“长度失衡导致某种参数几何、该几何进而导致能力退化”的完整因果链，引言不应暗示已证明这条链。

## 2. 其他论文怎样组织引言

以下为对原文叙事结构的编辑性归纳。

| 论文与原文 | 引言顺序 | 对本稿的启发 |
|---|---|---|
| [MOPD, §1](https://arxiv.org/html/2606.30406v1#S1) | 各领域专家的进展 → 单模型整合需求 → 四类现有整合方案及不足 → MOPD → 结果与贡献 | 先让读者理解能力整合这个任务，再介绍实现方式。本文可将这段背景压缩，因为 MOPD 已是既有方法。 |
| [Open-MOPD, §1](https://arxiv.org/html/2608.19098v1#S1) | MOPD 的价值 → 机制和可复现方案的缺口 → 受控实验与具体整合失败 → 诊断 → 方法和贡献 | 用具体、可测的现象收紧研究缺口；不能仅停在“optimization dynamics remain poorly understood”。 |
| [Dense Supervision, Sparse Updates, §1](https://arxiv.org/html/2606.13657v3#S1) | OPD 的位置 → SFT/RL 已知的参数行为 → OPD 带来的具体疑问 → 分析方式 → 核心发现与功能验证 | 最接近本稿的分析型叙事：先提出统一问题，再用结果回答。 |
| [On the Geometry of On-Policy Distillation, §1](https://arxiv.org/html/2606.07082v3#S1) | 后训练背景 → 已知的 SFT/RL 几何差异 → OPD 的未解问题 → 三项问题对应的答案 | 可以保留三问结构，但各项应预告发现。当前稿的列表主要告诉读者测量了什么。 |
| [GKD, ICLR 2024, §1](https://proceedings.iclr.cc/paper_files/paper/2024/file/5be69a584901a26c521c2b51e40a4c20-Paper-Conference.pdf) | 部署需求与蒸馏 → 既有方法的分布和容量问题 → 方法逐一回应 → 实验与贡献 | 相关工作、gap 与本文答案应一一对应；系统评估设计选择也可以成为贡献。 |

## 3. 当前引言的具体问题

1. **进入技术细节太快。** 第一段刚解释学生生成、教师评分，就转到回答长度；读者还没理解 MOPD 的已有进展和本文定位，便进入第一项实验。
2. **三个问题缺少共同动机。** 第二至四段分别提出 averaging、teacher overlap、supervision density，但没有先解释它们怎样共同帮助理解能力整合。
3. **贡献列表接近目录。** “We derive / measure / compare”交代研究活动，未突出推导和实验带来的新认识。
4. **贡献重心与完整证据不完全一致。** 第二项将 teacher overlap 放在显眼位置，但共同状态下逐教师真实 optimizer step 的比较尚未完成。目前最扎实的结果是联合训练的 FP32/BF16 参数变化及相应控制。

因此，建议把当前第二至四段重组为“已有进展”和“剩余问题”两段，把三条列表改成结果导向的贡献。

另一个需要同步调整的概念是：**loss averaging 和 vocabulary supervision 是训练选择，update sparsity 是被测性质。** 不宜像当前摘要一样将三者并列为 three core choices；应将参数变化作为解释和评价监督选择的一组测量。

## 4. 怎样区分已有工作与本文贡献

**第一项：突出域内 response weighting。** Open-MOPD 已识别长度失衡并提出 token-share balancing。按相同 batch、有效 token mask 与目标域权重代入其 §4.1 Eq. (8)，该修正对应本稿的 DT；这是代数对应关系。本文进一步区分 DT 与 DR，展示即使域权重相同，域内长度加权仍改变梯度方向，并测量平均表现与最差领域退化的取舍。不能把“首次发现等 prompt 不等于等 token 权重”作为新意。[外部依据](https://arxiv.org/html/2608.19098v1#S4.SS1)；本稿依据：`sections/005_normalization.tex:26–55`。

**第二项：突出联合训练中的实际测量与解释。** 既有工作已经限定 checkpoint precision，并讨论有限精度对可见变化的影响。本文应描述对 MOPD 的扩展、FP32 optimizer weights 与 BF16 model weights 的配对测量，以及单步和累计变化的区别。仅凭当前结果，不宜声称推翻了此前 OPD 稀疏性结论，也不能宣称发现了独立教师子网。[外部依据一](https://arxiv.org/html/2606.13657v3#S1)、[外部依据二](https://arxiv.org/html/2606.07082v3#S3.SS1)；本稿依据：`sections/006_teacher_subnetworks.tex:13–18`。

这一项目前更接近有价值的实证扩展与测量澄清。若希望将“共享学生内的教师作用”写成较强的新贡献，需要完成同一学生和 optimizer 状态下的逐教师 step 比较，依据结果再决定措辞。

**第三项：突出局部更新集中度与最终能力的区别。** 原 MOPD 已比较 PG 与 top-k，因此“我们比较两种 loss”本身不足以构成鲜明贡献。本文的具体价值是把同状态下的局部监督比较与在线能力轨迹放在一起：局部更新集中度非常接近，最终能力仍有差异。这里的 I64 是 student/teacher top-64 intersection，不能与任意 top-k 公式等同；也不能把同状态局部实验写成已完全解释在线收益的机制。[原 MOPD 的比较](https://arxiv.org/html/2606.30406v1#S4.SS4.SSS1)；本稿依据：`sections/007_supervision_density.tex:8–33`。

## 5. 推荐的段落安排

| 段落 | 回答读者的问题 | 建议内容 |
|---|---|---|
| 第一段 | MOPD 是什么，为什么值得研究？ | 第一句说明工业界采用 MOPD 做能力合板；第二句列出四个具体系统及其报告；随后解释学生生成、教师评分和共享学生的能力整合。 |
| 第二段 | 最近工作已经推进到哪里？ | MOPD 的能力整合；Open-MOPD 的能力失衡与分配机制；OPD 参数几何分析。约一段，按问题串联。 |
| 第三段 | 这些进展后，还有什么具体问题？ | 域平衡不能决定域内响应权重；可见参数稀疏性不能替代实际优化过程；监督覆盖和局部集中度不能替代最终能力评价。用统一问题收束。 |
| 第四段 | 本文怎么回答？ | 说明是 Qwen3-1.7B、四领域的受控研究，结合固定状态诊断与在线训练比较；点明后文三项贡献。 |
| 贡献列表 | 本文得到什么？ | 每条先给发现，再用最必要的实验事实支撑。避免重复一遍实验设置。 |
| 结尾 | 这些发现如何帮助实践？ | 将 recipe 写作基于现有证据的候选方案，强调显式权重、监督选择与逐域保留目标。 |

建议英文总长约 500–650 词。引言中无需展开梯度协方差公式、Adam 处理步骤、top-k 的全部变体或未来实验清单。Related Work 继续承担更细的比较。

## 6. 英文 introduction 草稿

以下草稿的既有引用使用现有 BibTeX keys；方括号中的 cite key 在采用时替换为 `\citep{...}`。新增工业报告在第一段保留可直接核读的文献链接，其来源和引用状态见第 8 节。这里直接采用当前完整实证范围。

Multi-teacher on-policy distillation (MOPD) is increasingly used in industrial LLM post-training to consolidate specialized capabilities into a single model. Recent examples include Xiaomi's [MiMo-V2-Flash](https://arxiv.org/html/2601.02780v2#S4.SS1), NVIDIA's [Nemotron 3 Ultra](https://arxiv.org/html/2606.15007v1#S3.SS3), [Kimi K3](https://arxiv.org/html/2607.24653#S4.SS1.SSS3), and [DeepSeek-V4](https://arxiv.org/html/2606.19348#S5.SS1.SSS2). In this paradigm, specialized teachers provide token-level feedback on responses generated by the student itself, allowing independently developed experts to transfer their capabilities to a shared student. This design supports capability integration across diverse domains while adapting the supervision to the student's evolving behavior.

Recent work has advanced both the practice and analysis of this approach. MOPD demonstrates capability integration using policy-gradient and top-k distillation losses [ma2026mopdmultiteacheronpolicydistillation]. Open-MOPD identifies uneven capability transfer and develops mechanisms for balancing domain token shares, allocating supervision according to student–teacher gaps, and refreshing student-dependent rewards [gao2026openmopddiagnosingfixingcapability]. Complementary studies characterize the sparsity and geometry of OPD parameter changes, including their concentration at checkpoint precision and their evolution during training [yu2026denseupdates, shen2026opdgeometry]. These results motivate examining how supervision choices translate into parameter changes and capability outcomes in a shared student.

Several distinctions are central to this examination. Balancing domain weights still leaves open how responses of different lengths are weighted within each domain. Concentrated changes in stored model weights do not, by themselves, describe the breadth of optimizer updates. Likewise, supervising more vocabulary alternatives need not produce a proportional increase in changed parameters, and similar local update concentration may accompany different capability outcomes. The resulting question is: how do loss averaging and vocabulary supervision affect learning in MOPD, and what do parameter-change measurements reveal about these choices?

We investigate this question through controlled comparisons with a Qwen3-1.7B student and four domain teachers covering mathematics, code, instruction following, and science. We combine algebraic analysis of loss averaging, local measurements at fixed student and optimizer states, and capability evaluations along training. Our contributions are threefold:

1. **Separating domain balancing from response weighting.** We distinguish global-token, domain-token, and domain-response averaging and show that within-domain length weighting changes gradient direction even when domain weights are identical. At the common update-50 comparison, global-token averaging achieves the highest mean score, while domain-response averaging yields the smallest worst-domain decline from initialization. The choice therefore depends on the intended capability tradeoff.

2. **Characterizing parameter changes across numerical representations.** We measure individual optimizer steps and cumulative changes separately in FP32 optimizer weights and BF16 model weights. At update 250, the two joint-training branches change only 5.9–7.1% of BF16 coordinates but 95.7–96.1% of FP32 coordinates cumulatively. These measurements clarify how sparse stored changes coexist with broad optimization activity in MOPD.

3. **Comparing vocabulary supervision through both updates and capability.** At a shared state, sampled-token, top-k, and full-vocabulary losses produce similar local update concentration. Online training nevertheless yields different capability profiles: at the matched update-250 checkpoint, student/teacher top-64 intersection supervision scores higher than sampled-token supervision in three domains and ties on instruction following. The mathematics-only teacher setting retains cross-domain tradeoffs, motivating evaluation beyond local sparsity alone.

We use these findings to outline a candidate MOPD recipe combining explicit supervision weights, vocabulary feedback selected using capability evaluations, and checkpoint selection under per-domain retention constraints. Its combined effectiveness and stability remain to be validated.

## 7. 与 abstract 同步时需要校准的表述

- **模型规模：**当前完整主比较使用 Qwen3-1.7B、训练 seed 42，见 `sections/005_experiment.tex:8–12`。历史 SmolLM3-3B 管线不支持将现有三问写成已在 1.7B 和 3B 两个规模完成验证。
- **教师重叠：**`sections/006_teacher_subnetworks.tex:38,44` 明确标注逐教师 optimizer-step overlap 及配对 JS 未完成。附录原始梯度的 routed mean cosine 接近零，见 `sections/009_appendix.tex:69`；不能写 “updates induced by different teachers are strongly aligned and overlapping”。
- **监督密度：**当前局部正文测的是 optimizer-step concentration，不能在摘要中不加说明地改称 per-batch gradient sparsity。更宽词表的局部比较与在线 I64 比较也应区分。
- **监督目标：**PG 与 I64 同时涉及选集、截断和权重差异。现有比较支持具体监督方案之间的差异，不能声称已单独识别词表“密度”这一因素的因果作用。
- **recipe：**`sections/007_recipe.tex:20` 标明整体验证待完成。引言可预告候选方案和实践意义；现有 PG/I64 的分数差不能写成完整 recipe 的性能提升。
- **保留能力：**现有 worst-domain Δ 是相对初始学生，不等于相对各域教师的能力继承率。引言和摘要需要使用相同参照。

这些校准会让 introduction 的贡献承诺与后文读者实际能看到的证据一致。

## 8. 工业界 MOPD 能力合板：原文核验与开头措辞

四个实例都支持“用 MOPD 将专门教师的能力整合进单个模型”。英文建议用 **capability consolidation** 或 **consolidating specialized capabilities into a single model** 表达这里的“合板”。这些报告描述的是通过教师反馈完成能力整合，使用该术语能清楚表达本文关心的对象。

| 系统 | 原文位置 | 可支持的表述 |
|---|---|---|
| Xiaomi MiMo-V2-Flash | [Technical Report, §4.1 Stage 3，及 §4.4](https://arxiv.org/html/2601.02780v2#S4.SS1) | 专门教师在学生生成轨迹上提供逐 token 反馈，通过 MOPD 整合多个领域的能力。 |
| NVIDIA Nemotron 3 Ultra | [Technical Report, §1，§3.3](https://arxiv.org/html/2606.15007v1#S3.SS3) | 使用十余个领域教师，通过 MOPD 将其能力整合进 Ultra；应具体注明 Ultra，而非笼统指整个 Nemotron 系列。 |
| Kimi K3 | [Kimi K3: Open Frontier Intelligence, §4.1.3](https://arxiv.org/html/2607.24653#S4.SS1.SSS3) | 使用 MOPD 将不同领域和推理 effort 下的九个专家整合进统一模型。 |
| DeepSeek-V4 | [Technical Report, §5.1.2](https://arxiv.org/html/2606.19348#S5.SS1.SSS2) | 明确将 multi-teacher OPD 作为专家能力合板的主要技术，十余个教师向单个学生提供监督。 |

上述是共同的应用层描述，不意味着四套系统使用相同 loss、调度或词表监督实现。第一段列举应用即可，技术差异放到后续相关研究中。

Nemotron 3 Ultra 还可用于强化第三问的动机：[§3.3.5](https://arxiv.org/html/2606.15007v1#S3.SS3.SSS5) 报告其初步 top-k/full-vocabulary matching 未优于 sampled-token objective，并在部分 agentic benchmarks 上更差。报告明确把更广分布监督何时有益列为未解问题。这里可以引出本文对具体监督方案、局部更新与在线能力的比较；不能声称本稿的小模型四域实验已经解释了其 agentic 结果。

引用状态：当前主 `.bib` 已包含 Kimi K3 的 `kimiteam2026kimik3openfrontier` 和 DeepSeek-V4 的 `deepseekai2026deepseekv4highlyefficientmilliontoken`。正式采用时还需加入 MiMo-V2-Flash Technical Report（arXiv:2601.02780）和 Nemotron 3 Ultra 报告（arXiv:2606.15007）的条目。本次更新的是讨论草稿，未改主论文或其参考文献库。
